const {normalizeAnalysis}=require('./normalize-analysis');
const schema={type:'object',additionalProperties:false,required:['summary','palette','composition','textures','tags','prompt'],properties:{summary:{type:'string'},palette:{type:'array',items:{type:'object',additionalProperties:false,required:['hex','role'],properties:{hex:{type:'string'},role:{type:'string'}}}},composition:{type:'object',additionalProperties:false,required:['lightType','tone','framing','composition','depth'],properties:{lightType:{type:'string'},tone:{type:'string'},framing:{type:'string'},composition:{type:'string'},depth:{type:'string'}}},textures:{type:'array',items:{type:'string'}},tags:{type:'array',items:{type:'string'}},prompt:{type:'string'}}};
async function analyzeImage({openAI,buffer,mimeType,locale='en',signal}){const language=locale==='zh'?'Simplified Chinese':'English';const response=await openAI.responses.create({model:process.env.OPENAI_MODEL||'gpt-5.6-luna',instructions:`You are a senior visual design analyst. Return precise visual observations in ${language}. Do not identify people. Produce a reusable image-generation prompt.`,input:[{role:'user',content:[{type:'input_text',text:'Analyze this image visual style: palette, composition, light, texture, mood, tags, and a complete generation prompt.'},{type:'input_image',detail:'high',image_url:`data:${mimeType};base64,${buffer.toString('base64')}`}]}],text:{format:{type:'json_schema',name:'visual_style_analysis',strict:true,schema}}},{signal});let parsed;try{parsed=JSON.parse(response.output_text);}catch{const error=new Error('Invalid model output');error.code='INVALID_MODEL_OUTPUT';throw error;}return normalizeAnalysis(parsed,locale);}
module.exports={analyzeImage,schema};
